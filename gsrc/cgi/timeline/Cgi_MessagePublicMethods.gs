package cgi.timeline

/**
 * Author: ankit.nandanwar@cgi.com
 * <p>
 * Structure, implemented by the OOTB Message entity, that acts like a bridge between the OOTB Message entity and the
 * custom 'messaging' implemention class (Cgi_MessageImpl.gs). This is done so as to provide a way to override the OOTB
 * messaging methods (through the custom class).
 */
structure Cgi_MessagePublicMethods {
}